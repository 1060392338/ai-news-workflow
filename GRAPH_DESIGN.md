# 工作流图设计

## 整体结构 (单图 + 并行子图)

```
                    START
                      │
              route_by_stage (从 checkpoint 恢复)
              ┌───────┼───────┐
              ▼       ▼       ▼
          [collect] [gen]  [publish]
              │       │       │
              ▼       ▼       ▼
         [aggreg]  [review]  END
              │       │
              ▼       ▼
         [push10] [push_pre]
              │       │
          ╔══╧══╗  ╔══╧══╗
          ║中断1║  ║中断2║
          ╚══╤══╝  ╚══╤══╝
     用户选择  │  用户确认
          ┌───┘   ┌───┘
          ▼       ▼
        [gen]   [publish]
```

## 搜索并行子图

```
     search_subgraph
    ┌─────────────────────────────┐
    │  fan_out (并发启动 5 路)     │
    │  ┌──────┐ ┌──────┐ ┌────┐  │
    │  │github│ │  hn  │ │... │  │  ← 每路独立重试x2
    │  │retry │ │retry │ │retry│  │
    │  └──┬───┘ └──┬───┘ └──┬─┘  │
    │     └────┬───┘────────┘     │
    │         collect_results     │  ← 合并+报告
    └─────────────────────────────┘
```

## 各节点的兜底策略

| 节点 | 重试 | 兜底 |
|------|------|------|
| search_* | 重试 2 次，间隔 2s/4s | 失败=空结果，不影响其他源 |
| generate | 重试 1 次 + 简化 prompt 重试 | 模板填充（摘要扩展） |
| publish | 重试 3 次 | 截图保存+记录失败+降级标记 |
| aggregator | 不重试 | 如果全部为空→报错终止 |
| reviewer | 不重试 | 违规内容自动截断+标记 |

## 状态机的恢复机制

```
run() 每次调用:
1. load_state() → 获取上次 stage
2. 根据 stage 路由到对应节点
3. 执行到下一个中断点 (push_top10 / push_preview)
4. save_state() → 持久化
5. 返回 _message (飞书消息)
```

## 异常安全

每个节点内部 try/except 包裹，异常不会崩掉整个图。
所有异常记录到 state["errors"] 列表。
